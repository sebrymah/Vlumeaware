node_modules/
dist/
.next/
.env
.env.local
.local-storage/
*.log
.DS_Store
coverage/
# TypeScript incremental build caches: regenerated by every typecheck and build,
# so tracking them only adds diff noise and merge conflicts.
*.tsbuildinfo
